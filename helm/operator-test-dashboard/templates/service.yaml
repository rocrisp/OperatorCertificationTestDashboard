apiVersion: v1
kind: Service
metadata:
  name: {{ include "operator-test-dashboard.fullname" . }}
  labels:
    {{- include "operator-test-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "operator-test-dashboard.selectorLabels" . | nindent 4 }}
